# Today

A small, dependency-free todo list that persists in the browser.

```bash
npm test
npm start
```

Then open http://localhost:8000.

Project-local agent skills are stored in `.agents/skills`:

- `backend-engineer`
- `caveman`
- `ponytail`
